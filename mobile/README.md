# Ayumi — Mobile App

This directory will hold the React Native + Expo mobile application.
It shares the same Zustand stores and Neon service layer as the desktop app.

## Setup (future)
```bash
npx create-expo-app@latest ./
```

The common logic layer (`stores/`, `services/`) will be linked or copied here.
